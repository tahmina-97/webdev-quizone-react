import React, { useContext } from 'react';
import { TopicsContext } from './Root';

const Statistics = () => {
    // const subjects = useContext(TopicsContext);
    // console.log(subjects);
    return (
        <div>
            <h1>This is statistics</h1>
        </div>
    );
};

export default Statistics;